/* Module */
var chai = require('chai');
var dotaWebAPI = require('./api');

/* Chai API */
var assert = chai.assert;
var expect = chai.expect;
var should = chai.should();

var myAPIKey = "1F72FF9BF1B0C1DF25A8CFA722DA0019";
var api = new dotaWebAPI(myAPIKey);

describe("getMatchDetails()", function() {
	it("should return a valid response containing the correct properties of the JSON object", function(done) {
		
	})
})
